import { type Metadata } from "next";
import { siteTitle } from "./_shared/const";
import RootLayout from "./_shared/rootLayout";

export const metadata: Metadata = {
    title: siteTitle,
};

export default RootLayout;
